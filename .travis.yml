language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t dezyntechnologies/docker-react -f Dockerfile.dev .
  

script:
  - docker run -e CI=true dezyntechnologies/docker-react yarn test